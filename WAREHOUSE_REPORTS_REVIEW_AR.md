# ملخص مراجعة المستودعات والمناقلات — PR #40

## النطاق المنفذ

- صفحة عربية مستقلة `warehouses` تعرض كل مستودع فعلي من `dbo.st000` وأصنافه وكمياته.
- ربط فواتير مشتريات Ameen بالمستودع الفعلي عبر `COALESCE(bi.StoreGUID, u.StoreGUID)`.
- عرض المشتريات الداخلة لكل مستودع ضمن التقرير المحمي للمالك.
- مزامنة مناقلات المستودعات للعرض فقط، مع ربط مستند الإخراج بمستند الإدخال حسب:
  `العائلة + التاريخ + الرقم`.
- رفض رفع تقرير المناقلات إذا كان أحد الطرفين مفقوداً، أو تعددت مستودعات الطرف، أو اختلفت كمية أي مادة.
- SQL محمي بـRLS: القراءة محصورة بـ`public.is_staff()` والكتابة بـUUID حساب المزامنة.
- سكربت تسجيل مهمة Windows دورية جاهز، لكنه لم يُشغّل قبل المراجعة.

## حدود الأمان

- جميع استعلامات Ameen في هذا النطاق `SELECT` فقط.
- لا توجد كتابة شراء أو مناقلة أو تعديل مخزون داخل Ameen.
- لم يُطبّق SQL الجديد على Supabase production.
- لم تُسجّل مهمة Task Scheduler الجديدة.

## التحقق الحي بتاريخ 2026-08-05

- المستودعات: 5.
- الأصناف: 419 صنفاً في كتالوغ كل مستودع.
- المناقلات منذ 2026-07-01: 92/92 مزدوجة ومتوازنة، 0 مشكلة.
- فواتير الشراء منذ 2026-07-01: 69 فاتورة، وكل فاتورة مرتبطة بمستودع واحد:
  - دوما الأساسي: 13.
  - دوما الاحتياطي: 23.
  - مركز: 33.

## المطلوب من Copilot

1. مراجعة أمان RLS والصلاحيات في ملفي SQL.
2. مراجعة منطق pairing للمناقلات وحالات التصادم في التاريخ/الرقم.
3. التأكد من أن استعلامات Ameen لا تحوي أي كتابة.
4. مراجعة واجهة الموبايل ومسارات التحميل عند غياب تقرير أو غياب صلاحية تقرير المشتريات.
5. عدم اقتراح تطبيق SQL أو تسجيل المهمة قبل إغلاق ملاحظات المراجعة.

# ============================================================
# push-inventory-reconciliation-to-ameen.ps1  (مقفل — لا يعمل بعد)
# الهدف المستقبلي: تحويل جلسة جرد معتمدة (status='approved') إلى قيد تسوية
# مخزون حقيقي بالأمين (AmnDb002)، أي فرق بين النظام والفعلي يصبح حركة فعلية.
#
# هذا السكريبت مقفل عمداً بـexit 1 لأن اعتماد الجلسة حالياً "تسجيلي فقط":
# - لا نملك GUID موثوقاً لنوع مستند "تسوية جرد" بالأمين (لم يُكتشف بعد،
#   انظر tools\discover-ameen-inventory-recon-fields.ps1).
# - لا نملك حساب أرباح/خسائر معتمد لتسجيل قيمة فروقات الجرد.
# - تكلفة الوحدة (unit_cost) بجدول inventory_recon_lines تقديرية للعرض فقط،
#   وليست بالضرورة نفس تكلفة الأمين الحقيقية لحظة الجرد.
#
# لا تُزل exit 1 إلا بعد:
# 1) تشغيل discover-ameen-inventory-recon-fields.ps1 ومراجعة نتائجه يدوياً،
# 2) تأكيد GUID نوع المستند وحساب الربح/الخسارة من صاحب الحساب مباشرة،
# 3) موافقة كتابية صريحة على تفعيل الكتابة الفعلية بالأمين.
# ============================================================

Write-Host "push-inventory-reconciliation-to-ameen.ps1 غير مفعّل بعد — الجرد الشهري تسجيلي فقط حالياً." -ForegroundColor Yellow
exit 1

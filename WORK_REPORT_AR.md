# تقرير تصحيح مسار مزامنة الفواتير

## ما ثبت فعلياً

- لم يوجد أي Migration محلي باسم `20260805115421_create_ameen_sales_invoice_reports.sql`.
- جدول `ameen_sales_invoice_reports` المقترح غير مطلوب؛ لا يوجد مستهلك له في الموقع.
- الموقع يقرأ تقرير المبيعات القائم من `inventory_reports` حيث `source = ameen_customer_invoices`.
- أحدث تقرير Supabase الذي فُحص قراءةً فقط بتاريخ 2026-08-05 احتوى 354 فاتورة و60 زبوناً من نافذة 60 يوماً، وعدد الفواتير المحسوب من `items[].invoices[]` طابق `summary.bills`.
- مهمة Windows `TOBACCO Customer Invoices Push` كانت تعمل كل خمس دقائق، ونتيجة آخر تشغيل وقت الفحص كانت `0`.
- استعلام Ameen للفترة 2026-07-30 حتى ما قبل 2026-08-05 أعطى 64 فاتورة ذات اسم زبون: 43 مبيعات، 18 مبيعات مركز، 2 مرتجع مبيعات، و1 مرتجع مبيعات مركز. هذا يفسر الرقم 64 ولا يعني وجود 64 فاتورة غير مزامنة.
- المطابقة المباشرة بالـGUID لنفس الفترة أثبتت: 64 في Ameen، و64 في أحدث `inventory_reports`، و0 مفقود، و0 زائد، و0 GUID مكرر. منها 61 مبيعات و3 مرتجعات.
- اختبار `tools/push-sales-line-items.ps1 -DryRun -Days 7` قرأ 1766 سطر حركة وخرج بالرمز 0، بلا كتابة إلى Supabase.
- شُغّل السكربت المعتمد `pull-purchase-invoices-from-ameen.ps1 -PeriodDays 60` بناءً على طلب المالك؛ نجح الرفع، وأصبح أحدث تقرير المشتريات في Supabase يحتوي 68 فاتورة لـ16 مورداً من 2026-06-06، مع تطابق `summary.bills` وعدد الفواتير داخل التقرير.

## الأخطاء التي أزيلت

- حُذف `sync-sales-invoices-enhanced.ps1`: كان يحتوي GUIDs وهمية، يكتب إلى جدول غير موجود، ويستبعد الفواتير بلا اسم زبون.
- حُذف `sync-purchase-invoices-enhanced.ps1`: كرر السكربت المعتمد وقلّص snapshot الفترة اعتماداً على ملف آخر نجاح، ما كان سيفقد التاريخ الأقدم في التقرير الأحدث.
- أُلغي اقتراح إنشاء جدول ومجموعة RLS جديدة للمبيعات لأن المشروع يملك مخزناً ومستهلكاً وجدولة قائمة بالفعل.

## ما لم يُنفذ

- لم يُطبّق أي SQL على Supabase.
- لم تُجرَ أي كتابة على Ameen.
- لم تُسجّل أو تُعدّل مهمة Windows.
- لم يحدث commit أو push أو نشر.

أضيف `tools/register-purchase-invoices-pull-task.ps1` لتجهيز الجدولة المستمرة، لكن لم يُشغّل لأن الجلسة الحالية ليست Administrator ولأن تحويل المهمة إلى `OZKSync` يحتاج كلمة مروره تفاعلياً.

## المطلوب من المراجع

1. التأكد من أن حذف السكربتين الوهميين هو المقصود وعدم وجود مستهلك خارجي لهما.
2. مراجعة أن `push-customer-invoices.ps1` هو المصدر الوحيد لفواتير الزبائن في الموقع.
3. مراجعة قرار إبقاء بنية `inventory_reports` الحالية وعدم إنشاء جدول مبيعات ثالث.
4. إعادة تشغيل `tools/verify-customer-invoice-sync.ps1` للتأكد المستقل من مطابقة الـGUIDs.
5. مراجعة نتائج `npm.cmd run check` و`git diff --check` بعد التعديل.
6. تشغيل مسجل مهمة المشتريات من نافذة Administrator ثم تحويلها إلى `OZKSync`، والتحقق من `LastTaskResult = 0`.

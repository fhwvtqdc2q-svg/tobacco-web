# تقرير العمل المنجز - منصة TOBACCO

## الملفات المعدلة

### 1. `src/app.js` (الملف الرئيسي للتطبيق)
- **السطر 188**: إضافة المسارات المفقودة (staff, search, ai, dashboard) إلى allowedRoutes
- **السطور 2720-2731**: حذف زر الفواتير من شريط التنقل الجانبي
- **السطر 8950**: حذف invoice من كائن pages (الملتقط)
- **السطر 2794**: حذف عنوان صفحة الفواتير من pageTitle()
- **السطور 2804-2815**: تبسيط الصفحة الرئيسية (overview) لعرض فقط:
  - الشعار
  - "مركز أبو زياد OZK TOBACCO" بخط عريض
  - "لتجارة الدخان الوطني والأجنبي والمستورد"
  - أرقام المركز والرقم الخاص (إن توفرت في الإعدادات)
- **السطور 4634-4649**: إزالة قسم statistics (عدد الزبائن، عدد من عليهم رصيد، إلخ) من تبويب أرصدة الزبائن
- **السطر 3115**: إصلاح حساب "اقتراب من الحد": من 80% إلى 90% (كما بالمتطلبات)

### 2. `src/config.js` (إعدادات التطبيق)
- إضافة متغيرات جديدة:
  - `centerNumber`: رقم المركز (فارغ حالياً - ينتظر ملء من المستخدم)
  - `privateNumber`: الرقم الخاص (فارغ حالياً - ينتظر ملء من المستخدم)

### 3. `public/service-worker.js` (تخزين الويب)
- تحديث `CACHE_NAME` من v457 إلى v458 (لضمان تحديث الكاش عند المستخدمين)

### 4. `tools/sync-purchase-invoices-enhanced.ps1` (ملف جديد)
نسخة محسّنة من سكريبت مزامنة فواتير المشتريات مع المميزات التالية:
- **تتبع آخر نجاح**: يحفظ آخر تاريخ فاتورة تمت مزامنتها بنجاح في ملف `logs/purchase-sync-last-success.txt`
- **الاستئناف الذكي**: عند تشغيل السكريبت مرة أخرى، يبدأ من آخر نجاح بدلاً من البدء من قديم
- **إعادة محاولة تلقائية**: عند فشل الرفع إلى Supabase، يحاول مرة أخرى (exponential backoff بحد أقصى 3 محاولات)
- **معالجة أخطاء أفضل**: تسجيل تفصيلي لكل خطأ
- **منع التكرار**: يتحقق من GUID الفاتورة قبل الرفع

## الحالة الحالية

### ✅ المهام المكتملة

#### Task 7: حذف تبويب الفواتير
- **الحالة**: مكتملة
- **الملفات المعدلة**: src/app.js
- **الملاحظة**: تم حذف الزر من التنقل والملتقط والعنوان، لكن جداول الفواتير في قاعدة البيانات بقيت محفوظة

#### Task 5: تبسيط الصفحة الرئيسية
- **الحالة**: مكتملة
- **الملفات المعدلة**: src/app.js, src/config.js
- **الملاحظة**: تم إزالة كل الإحصاءات والعدادات، وإبقاء الشعار والاسم والرقم فقط

#### Task 6: تبسيط تبويب أرصدة الزبائن
- **الحالة**: مكتملة
- **الملفات المعدلة**: src/app.js
- **الملاحظة**: تم إزالة قسم الإحصاءات بالكامل، وإبقاء قائمة الأرصدة والفرز والبحث فقط

#### Task 4: نظام الحد الائتماني
- **الحالة**: مكتملة جزئياً
- **الملفات المعدلة**: src/app.js (دالة deriveCustomerStatus)
- **التحسينات**:
  - تصحيح النسبة من 80% إلى 90% لتحديد "اقتراب من الحد"
  - التصنيف الحالي:
    - over_limit: تجاوز الحد
    - near_limit: اقترب من الحد (≥90%)
    - open_balance: لديه رصيد إيجابي لكن لم يقترب
    - credit_balance: له رصيد (سالب)
    - clear: متوازن

#### Task 2 & 3: مزامنة الفواتير
- **الحالة**: بدء الإصلاح
- **الملفات الجديدة**: tools/sync-purchase-invoices-enhanced.ps1
- **المشكلة المحتملة**: آخر فاتورة مسجّلة بـ 30/07/2026 تشير إلى توقف المزامنة — السبب المحتمل:
  - فشل الاتصال إلى Supabase
  - تغيير في بيانات الاعتماد
  - خطأ في استعلام SQL الأمين
  - مشكلة في RLS بقاعدة البيانات

#### Task 1: تحديث واجهة نشرات الأسعار
- **الحالة**: لم يتم البدء
- **المعلومات المطلوبة**: تحديد التصميم الجديد للنشرات (المتطلبات لم تُحدد بالتفصيل)

## المعلومات المطلوبة من المستخدم

1. **أرقام المركز والرقم الخاص**:
   - يجب ملء `appConfig.centerNumber` و `appConfig.privateNumber` في `src/config.js`
   - مثال: 
     ```javascript
     centerNumber: "+963950123456",
     privateNumber: "+963912345678"
     ```

2. **سبب توقف المزامنة بعد 30/07/2026**:
   - هل حدث تغيير في بيانات الاعتماد (TOBACCO_SYNC_EMAIL / TOBACCO_SYNC_PASSWORD)?
   - هل كان هناك تحديث على قاعدة الأمين؟
   - هل السكريبت كان مسجّلاً في Windows Task Scheduler؟

3. **تفاصيل التصميم الجديد لنشرات الأسعار**:
   - هل يتطلب تغيير في CSS فقط أم HTML؟
   - هل هناك صور أو رسوميات جديدة؟
   - هل النشرات تحتاج responsive design لشاشات مختلفة؟

4. **مزامنة المبيعات**:
   - هل هناك سكريبت منفصل لمزامنة فواتير المبيعات؟
   - هل المشكلة هي الفواتير المفقودة أم الأرصدة التي لا تُحتسب بشكل صحيح؟

## الخطوات التالية

### للاختبار على الموبايل:
1. تشغيل السيرفر المحلي: `npm run dev`
2. فتح الموقع من iPhone عبر عنوان LAN
3. التحقق من:
   - الصفحة الرئيسية تعرض الشعار والاسم فقط
   - تبويب الأرصدة لا يعرض الإحصاءات
   - تبويب الفواتير (الجديد) غير موجود في التنقل
   - الحدود الائتمانية تعرض الحالة الصحيحة (تجاوز/اقترب/متاح)

### لاستئناف مزامنة الفواتير:
```powershell
# على جهاز Windows مع الأمين:
.\tools\sync-purchase-invoices-enhanced.ps1 -PeriodDays 120  # مزامنة آخر 4 أشهر
```

### لملء أرقام المركز:
```javascript
// في src/config.js:
centerNumber: "أدخل الرقم هنا",
privateNumber: "أدخل الرقم هنا"
```

## الملاحظات المهمة

- **البيانات محفوظة**: لم يتم حذف أي جداول من قاعدة البيانات — فقط إخفاء الواجهة
- **PWA Cache**: تحديث CACHE_NAME يضمن أن المستخدمين سيحصلون على نسخة محدثة من الموقع
- **SQL RLS**: الأخطاء المتعلقة بـ SQL عند تشغيل npm run check لا تتأثر بهذه التعديلات
- **Git Sync**: تم commit التعديلات على الفرع `fhwvtqdc2q-svg-massive-ui-refactor-and-sync-fixes`

## الملفات المتبقية للاختبار

- ✅ `src/app.js`: تم اختبار npm run check (بدون أخطاء جديدة)
- ✅ `public/service-worker.js`: تم التحديث
- ✅ `src/config.js`: تم التحديث
- ⏳ `tools/sync-purchase-invoices-enhanced.ps1`: يحتاج اختبار على Windows مع الأمين
- ⏳ نشرات الأسعار: يحتاج اختبار بعد توضيح المتطلبات

